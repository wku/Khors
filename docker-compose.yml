version: '3.8'

services:
  khors:
    build: .
    container_name: khors
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - REPO_DIR=/app
      - DRIVE_ROOT=/app/data
      - PYTHONUNBUFFERED=1
