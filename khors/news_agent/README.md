# Автономный Новостной Агент

Автономный агент для сбора, анализа и доставки украинских новостей в Telegram.

## Возможности

- 🔄 **Автоматический сбор** новостей из RSS-лент и веб-источников
- 🧠 **LLM-анализ** важности и категоризация новостей
- 🎯 **Фильтрация** социально критичных событий
- ⏰ **Расписание** отправки дайджестов 4 раза в день
- 💾 **Хранение** данных с дедупликацией
- 📱 **Telegram** интеграция для доставки

## Архитектура

```
news_agent/
├── models.py           # Модели данных
├── news_collector.py   # Сбор новостей
├── news_analyzer.py    # LLM-анализ
├── news_storage.py     # Хранение данных
├── news_scheduler.py   # Планировщик дайджестов
├── news_sender.py      # Отправка в Telegram
├── news_agent.py       # Главный модуль
├── run_news_agent.py   # Скрипт запуска
├── config.yaml         # Конфигурация
└── tests/              # Тесты
```

## Установка

1. **Установите зависимости:**
```bash
pip install aiohttp feedparser beautifulsoup4 pyyaml
```

2. **Настройте переменные окружения:**
```bash
export NEWS_TELEGRAM_BOT_TOKEN="your_bot_token"
export NEWS_TELEGRAM_CHAT_ID="your_chat_id"
export OPENROUTER_API_KEY="your_openrouter_key"
```

3. **Создайте директорию для данных:**
```bash
mkdir -p data/news_agent
```

## Запуск

### Автономный режим
```bash
python khors/news_agent/run_news_agent.py
```

### Интеграция с Khors
```python
from khors.news_agent import NewsAgent, create_default_config

# Создание конфигурации
config = create_default_config(
    telegram_bot_token="your_token",
    telegram_chat_id="your_chat_id",
    llm_api_key="your_key"
)

# Запуск агента
agent = NewsAgent(config)
await agent.run_forever()
```

## Конфигурация

Основные настройки в `config.yaml`:

### Источники новостей
- RSS-ленты украинских медиа
- Веб-скрапинг официальных сайтов
- Настраиваемые селекторы и фильтры

### Расписание дайджестов
```yaml
digest_schedule: [6, 12, 18, 22]  # UTC часы
```

### Анализ важности
```yaml
analysis:
  min_importance_for_digest: "medium"
  max_news_per_digest: 15
  max_critical_news_per_digest: 5
```

## Использование

### Команды управления

Агент поддерживает команды через Telegram:

- `/news_status` - статус агента
- `/news_digest` - создать дайджест вручную
- `/news_stop` - остановить агента
- `/news_start` - запустить агента

### Мониторинг

Агент отправляет уведомления о:
- Запуске/остановке
- Ошибках сбора
- Статистике анализа
- Проблемах с подключением

## Источники новостей

По умолчанию настроены следующие источники:

### RSS-ленты
- Українська правда
- BBC Україна  
- Радіо Свобода
- Дзеркало тижня
- Економічна правда
- Цензор.НЕТ
- УНІАН
- Детектор медіа

### Веб-источники
- Офіс Президента України
- Міністерство оборони України

## LLM-анализ

Каждая новость анализируется по критериям:

### Важность
- `critical` - критически важные события
- `high` - высокая важность
- `medium` - средняя важность  
- `low` - низкая важность

### Категории
- `politics` - политика
- `military` - военные события
- `economy` - экономика
- `society` - общество
- `international` - международные новости
- `technology` - технологии
- `culture` - культура
- `sports` - спорт
- `general` - общие новости

### Фильтрация

Автоматическая фильтрация:
- Дубликатов по URL и содержанию
- Рекламы и спама
- Коротких новостей (< 100 символов)
- Исключенных источников

## Хранение данных

### База данных SQLite
- Сырые новости
- Проанализированные новости
- Дайджесты
- Метаданные и статистика

### Очистка данных
- Автоматическое удаление данных старше 30 дней
- Ротация логов
- Оптимизация базы данных

## Безопасность

- Валидация входных данных
- Ограничения на размер контента
- Таймауты для внешних запросов
- Обработка ошибок и восстановление

## Мониторинг и логи

### Логирование
```
data/news_agent/news_agent.log
```

### Метрики
- Количество собранных новостей
- Успешность анализа
- Время выполнения операций
- Ошибки и исключения

## Тестирование

```bash
# Запуск тестов
python -m pytest khors/news_agent/tests/

# Тест с покрытием
python -m pytest khors/news_agent/tests/ --cov=khors.news_agent
```

## Расширение

### Добавление источников

1. **RSS-лента:**
```yaml
news_sources:
  rss_feeds:
    - name: "Новый источник"
      url: "https://example.com/rss"
      category: "general"
```

2. **Веб-источник:**
```yaml
news_sources:
  web_sources:
    - name: "Новый сайт"
      url: "https://example.com/news"
      selector: ".news-item"
      category: "politics"
```

### Кастомный анализ

Переопределите метод `analyze_news` в `NewsAnalyzer`:

```python
class CustomNewsAnalyzer(NewsAnalyzer):
    async def analyze_news(self, news: RawNews) -> Optional[AnalyzedNews]:
        # Ваша логика анализа
        pass
```

### Дополнительные каналы

Добавьте новые отправители, наследуя от базового интерфейса:

```python
class EmailSender:
    async def send_digest(self, digest: NewsDigest) -> bool:
        # Отправка по email
        pass
```

## Производительность

### Оптимизации
- Асинхронный сбор новостей
- Параллельный анализ через LLM
- Кэширование результатов
- Батчевая обработка

### Ограничения
- Максимум 50 новостей с источника за раз
- Таймаут 30 секунд на запрос
- Максимум 15 новостей в дайджесте

## Troubleshooting

### Частые проблемы

1. **Ошибка подключения к Telegram**
   - Проверьте токен бота
   - Убедитесь, что бот добавлен в чат

2. **Не собираются новости**
   - Проверьте доступность RSS-лент
   - Проверьте селекторы для веб-источников

3. **Ошибки LLM-анализа**
   - Проверьте API-ключ OpenRouter
   - Убедитесь в наличии кредитов

### Логи и диагностика

```bash
# Просмотр логов
tail -f data/news_agent/news_agent.log

# Проверка статуса
python -c "
from khors.news_agent import NewsAgent, create_default_config
import asyncio

async def check():
    config = create_default_config('token', 'chat_id')
    agent = NewsAgent(config)
    status = await agent.get_status()
    print(status)

asyncio.run(check())
"
```

## Лицензия

Часть проекта Khors. Смотрите основную лицензию проекта.